# Reusable Prompt: Audit, Fix, and Re-Audit

Use this prompt with the current source tree and the latest entry in `docs/audit/ten-cycle-remediation-report.md`:

> Continue the requested remediation cycle from the current repository state. First reproduce the recorded failure or verify the exact code path. Write a focused test for the user-visible behavior and run it before changing production code; preserve the expected failing output. Implement the smallest complete fix that resolves the root cause. Then perform a strict audit of the changed path, its callers, data boundaries, error cases, lifecycle, and neighboring behavior. Record the audit findings and a concrete follow-up fix prompt in the report before implementing that follow-up. Apply the follow-up with a failing regression where behavior changes, rebuild, and run the focused and relevant existing checks. Finish with a strong audit against the original finding and the strict-audit follow-up. Update the report with exact commands, outcomes, remaining limitations, and evidence. Never infer a pass from code inspection, never label estimates as exact, and do not close the cycle while a Critical or Important finding remains. Keep iterating until at least ten numbered cycles have each recorded both audits and their follow-up prompt/fix. After cycle ten, run the full available verification set, request an independent review, fix all Critical/Important findings, commit, and push the branch to the configured remote.

## Cycle 1 follow-up prompt

> The live observation protocol now works, but the strict audit found that closed tabs leave their live session in the service worker's in-memory manager. Add a regression that submits observations for two tabs, closes one tab through the registered `chrome.tabs.onRemoved` listener, and proves the closed tab no longer receives its session while the other tab remains intact. Also verify that a sender without a provider tab, a sender on a mismatched provider URL, and an oversized draft are rejected without mutating either session. Implement cleanup through `SessionManager.removeTab`, rebuild, run the focused regression, and record the strong audit result. Do not return a full storage snapshot from the content message path.

## Cycle 2 follow-up prompt

> The manager now stores one live session per tab, but strict review found that a negative integer tab id passes `Number.isInteger` and can enter the session map. Add a red regression proving the message boundary rejects `tab.id = -1`, and `SessionManager.setSession` rejects negative, non-safe-integer, and non-integer ids without changing existing sessions. Tighten the sender and manager validation, rebuild, rerun the complete focused live-capture regression, and record the strong audit evidence.

## Cycle 3 follow-up prompt

> Enter, native form submit, and click now share one capture path, but strict review found that the dedupe window can suppress a legitimate identical prompt after the user clears and retypes it quickly. Add a failing event regression for clear/retype of the same text, reset dedupe when composer input changes, and prove that duplicate send signals without an intervening edit still produce one capture. Rebuild, run the event and live-capture regressions, and record the strong audit evidence. Preserve Shift+Enter/IME handling and do not prevent the provider's default send action.

## Cycle 4 follow-up prompt

> Streaming completion now waits for stable text, but strict review found that the ten-minute timeout only runs during DOM updates. A request with no response or no further page mutations can remain pending indefinitely. Add a failing regression that submits a prompt, emits no response DOM update, advances past the ten-minute deadline, and proves pending state is abandoned with no commit. Schedule and clear a real deadline timer with the exchange lifecycle; retain the DOM-time check as a defense. Re-run quiet-stream, stop-control, cancel, navigation, timeout, and build checks, then record the strong audit.

## Cycle 5 follow-up prompt

> Provider pages can no longer read a full snapshot, but strict review found that they can still invoke privileged writes/actions, use the legacy persistent `capture-session` path, and submit usage events labeled as another provider. Add failing cases proving page-origin `save-preferences`, `import-data`, `capture-session`, `clear-local-history`, cloud controls, `notify`, and `toggle-overlay` are denied without state or external side effects; extension-owned UI calls must still work. Valid provider tabs may commit only events matching their sender provider, and unsupported tabs may not commit events. Make the content script initialize visibility from `siteEnabled` and `preferences.showOverlay`, hiding the mounted overlay when either disables it while preserving the existing fallback if settings are unavailable. Implement the guards and behavior, then rebuild and rerun live-capture, tab-view preference, submission, capture-state, and relevant privacy checks. Record exact red and green outcomes and finish with a strong audit of every worker message route and page-visible payload.

## Cycle 6 follow-up prompt

> The estimator now avoids a deterministic label when no encoder function is loaded, but strict review found that the default o200k encoder is also used for cl100k requests, the page-injected script cannot initialize the isolated content-script world, and active draft/capture calls omit the recognized model tokenizer. Add failing checks for both encodings against the pinned `gpt-tokenizer@4.0.0` exports; a known GPT-4o model with its encoder loaded; the same model with no encoder or an encoder that throws; an unknown OpenAI model; Claude/Gemini models; an attachment whose token contribution is unknown; and an attachment with only a rough page-count estimate. Load encoders in the extension's isolated content-script world before the accuracy engine, only on hosts that use OpenAI tokenization, and remove the web-accessible page-world route. Resolve tokenizer selection from a known model profile and pass it through live draft estimates and committed prompt/response counts. Reuse the actual per-text breakdown metadata in context accounting, retain prompt and response measurement results through capture, and mark the committed event approximate if either count falls back after an encoder error. Preserve unknown attachment contributions as unknown, never add them as zero, do not label a total containing estimated attachment tokens deterministic, and keep the overlay/context pressure unknown when the displayed subtotal is incomplete or the provider context is hidden. Describe deterministic counts as exact only for visible text, name the local package, and exclude hidden provider context; do not imply the package is an official OpenAI implementation. Never apply OpenAI tokenization to other providers or unknown models. Verify manifest timing/scope, both model-aware call paths, truthful failure metadata, typecheck, build, focused token/capture regressions, and measured counts; then record the strong audit and any remaining browser limitation.

## Cycle 7 follow-up prompt

> Both `Copy shorter` controls are now connected, but the first-pass candidate selector can choose transformations that corrupt fenced code, erase Markdown hard breaks, or rewrite meaning without preview. Restrict the one-click candidate to conservative whitespace cleanup that preserves fenced blocks byte-for-byte, repeated lines inside code, significant trailing spaces, and source composer contents. Prevent standalone deduplication from crossing code fences and mark deduplication/wording rewrites as not guaranteed to preserve semantics. Route popup and overlay copying through one helper that first tries the Clipboard API and has a tested fallback for extension/content-script contexts. Keep the UI truthful when there is no shorter candidate or clipboard failure. Add regressions for code indentation and repeated code lines, CRLF, Markdown hard breaks, no speculative phrase rewrites, clipboard API success/fallback/failure, and the overlay and popup wiring. Then typecheck, build, run existing capture/optimizer checks, and record the post-fix audit.

## Cycle 8 follow-up prompt

> The package script now checks literal manifest references, but strict review found it ignores unmatched wildcard resources, can accept `..` paths outside the extension root, and does not verify manifest assets in the finished ZIP. Resolve every manifest path under the canonical extension root; reject absolute paths and traversal; expand web-accessible-resource globs and require at least one matching file; validate required entries against the actual sorted package file list and then the ZIP contents. Build/validate a temporary archive first and only replace the requested output after successful verification. Keep errors actionable for direct packaging from a clean checkout. Extend the fixture regression to cover missing generated JS, missing glob matches, traversal, a complete successful package with every referenced entry, and preservation of an existing output on invalid input. Then run the documented release verifier, check archive reproducibility, typecheck/build, and record any platform-only limitations.

## Cycle 9 follow-up prompt

> The old production audit has only had its date/version/build count updated, while its unqualified “10/10 production-ready” verdict and stale claims remain. Replace it with a current-tree snapshot grounded in the checked-in code and commands run in this session. Derive manifest version and build-target count; remove unsupported claims about all baseline defects, zero `any` leaks, Chromium/backend test passes, and side-by-side optimizer previews. State the exact extension checks and browser used, label the provider page as a controlled fixture, and list live-provider, Chromium, backend, or other unverified scope explicitly. Add an audit consistency check for current version/build count, unqualified certification phrases, stale test counts, and required limitations; run it plus typecheck/build and relevant regression checks. Update the report with precise evidence and commit only when the claims and source agree.
